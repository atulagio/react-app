server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    # ✅ Serve static assets directly
    location /assets/ {
        try_files $uri =404;
    }

    # ✅ React routing
    location / {
        try_files $uri /index.html;
    }
}